fetch("/api/faqs").then(r=>r.json()).then(rows=>{document.getElementById("faqs").innerHTML=rows.map(x=>`<article><h3>${x.question}</h3><p>${x.answer}</p></article>`).join("");document.querySelectorAll(".faq h3").forEach(h=>h.onclick=()=>h.parentElement.classList.toggle("open"))});
document.getElementById("inquiryForm").addEventListener("submit",async e=>{e.preventDefault();const body=Object.fromEntries(new FormData(e.target));const r=await fetch("/api/inquiries",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});if(r.ok){e.target.reset();alert("Thanks — your inquiry was sent.");}});
